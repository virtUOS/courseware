Um die Selenium-Webdriver Unittests ausführen zu können benötigt man für Linux "pip". 
Mittels "pip" lässt sich leicht selenium installieren.

Für ubuntu würde das ganze so aussehen:

sudo apt-get install pip
pip install selenium

Für upgrades: 
pip install --upgrade selenium
