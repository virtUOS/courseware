define(['assets/js/author_view'], function (AuthorView) {
    'use strict';

    return AuthorView.extend({
        initialize: function (options) {
        },

        render: function () {
            return this;
        }
    });
});
