<ul>
  {{#answers}}
    <li>
      <input type="radio" name="{{name}}" value="{{index}}" id="{{blockid}}_exercise_{{exercise_index}}_answer_{{index}}">
      <label for="{{blockid}}_exercise_{{exercise_index}}_answer_{{index}}">
        {{{text}}}
      </label>
    </li>
  {{/answers}}
</ul>
